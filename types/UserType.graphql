type User {
    id: ID!
    name: String
}

type Query {
    hello(name: String): String!
    users: [User]!
}

type Mutation {
    createUser(name: String): User!
    deleteUser(id: String): User!
}
